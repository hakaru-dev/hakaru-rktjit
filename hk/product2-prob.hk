fn topic_prior array(prob):
  fn word_prior array(prob):
    fn z array(nat):
     fn w array(nat): 
      fn doc array(nat):
        fn docUpdate nat:
	  array zNew of size(topic_prior):
	    product i from 0 to size(topic_prior):
              product j from 0 to size(word_prior):
                topic_prior[i] * word_prior[j]